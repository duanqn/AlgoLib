#pragma once

#include "matrix.hpp"

namespace AlgoLib::Math{
    class SquareMatrixInterface{
        private:

        public:
        SquareMatrixInterface(){}

        virtual ~SquareMatrixInterface(){}
    };
}